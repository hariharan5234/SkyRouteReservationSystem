# SkyRouteReservationSystem
This project is a Java-based console application that manages airline flight reservations and passenger check-ins. The system allows airlines to add flights, register passengers, book seats, confirm payments, and generate boarding passes
